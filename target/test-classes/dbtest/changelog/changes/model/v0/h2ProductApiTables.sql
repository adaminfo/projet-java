CREATE TABLE product ( id INT GENERATED BY DEFAULT AS IDENTITY, code VARCHAR(50), name VARCHAR(100), description VARCHAR(2000), image VARCHAR(255), category VARCHAR(50), price DECIMAL(10, 2), quantity INT, internal_reference VARCHAR(50), shell_id BIGINT, inventory_status VARCHAR(20), rating DECIMAL(3, 2), created_at BIGINT, updated_at BIGINT);